# PR206 复审报告（2026-09-09）

复审范围：`GalTransl-stream` 两分支相对基点 c1c470b 的全部 diff（逐行人工审查）。
验证：A 分支 11/11、B 分支 5/5 测试实跑通过（临时 worktree，跑完已清理）。

## 结论：两分支无阻断性缺陷，可推送。

---

## 一、基础问题排查（参数缺失 / 依赖缺失 / 口径不一致 / 配置读取 / 接口对位）

| 检查项 | 结果 |
|---|---|
| A：`getAvgSentenceLengthThreshold()` 读取路径 | ✅ `projectConfig.get("problemAnalyze", {})`，与 DefaultProjectConfig 写入位置一致 |
| A：除零风险 | ✅ `n_number + 1 ≥ 1` 恒成立；`clean_len` 为 0 不触发 |
| A：`float(raw)` 异常覆盖 | ✅ TypeError/ValueError/OverflowError 全捕获（NaN→floor 抛 ValueError、inf→OverflowError，均回退 17） |
| A：前后端阈值口径 | ✅ 逐值推演一致（0.5/负数/小数/NaN/inf/"0x10"/"Infinity" 全部对齐，此前 25 项比对 + 本次复核无翻案） |
| A：server.py 目录描述 | ✅ 已改为「超过阈值」 |
| A：`onThresholdChange` 中 `prev ? {...} : prev` | ⚠️ config 为 null 时静默丢弃写入——但 Section 仅在 problemTypes 加载成功后渲染阈值 UI，config null 时整段不渲染，实际不可达。低风险 |
| B：`skip_check` 全链路覆盖 | ✅ CSentense 默认 False → Cache 写（仅真值）/读（bool 归一）→ Problem 跳过 → server rebuild（唯一 CSentense 构造点 server.py:1055 已赋值）→ api.ts 类型 → 前端 checkbox |
| B：`/cache/save` 取消勾选落盘 | ✅ 整文件重写 entries（非 append 合并），旧 `skip_check: true` 不会残留 |
| B：rebuild 两侧 idx 对齐 | ✅ 构建与回写均以 `post_src == ""` 同条件 continue，索引一致 |
| B：skip_check 与 problem 清理闭环 | ✅ find_problems 清空 tran.problem → rebuild 循环 `elif "problem" in e: del e["problem"]`，磁盘同步清除 |
| B：onEntryChange 签名放宽 `string \| boolean` | ✅ value 仅 spread 进 entry，tsc 通过，其他调用方兼容 |
| B：⏭ emoji | ✅ 现有 UI 已有 ⏳/💾/📂 等 pill 先例，符合惯例 |

## 二、集成 / 状态残留 / 竞态

1. **A：输入校验比后端严（可接受，方向安全）**
   前端 `validateAndCommitThreshold` 要求整数（`Number.isInteger`），后端接受 17.5 并 half-up 到 18。用户手改 yaml 写小数时：后端生效值 = 前端显示值（resolveThreshold 同样舍入为 18），仅显示值 ≠ yaml 原文。前端不会产生后端拒的值，无需改。

2. **A：type="number" 中间态误报（轻微 UX，可不改）**
   输入 "e"、"-" 等中间字符时浏览器返回空串，会闪现「阈值不能为空」错误提示，继续输入即消失。

3. **A：useEffect 依赖 [config]（已修正正确）** — 依赖 threshold 会在两项目同值时不重置，现写法正确。

4. **B：跨进程残留（理论边界，低风险）**
   取消勾选保存（整文件重写）若与**正在运行的翻译任务**并发，任务侧 append 日志中的旧条目仍含 `skip_check: true`，compaction 的 `merged_obj.update(cache_obj)` 会把 true 合回快照 → 勾选"复活"。但这属于「前端整文件保存 vs 运行中 append」的既有写冲突范畴（本轮已裁决为既有问题不修），非本改动引入。正常运行流程（翻译结束后再改缓存）不触发。

5. **B：`"false"` 字符串按 Python bool() 语义解析为真** — 已知决策点（原总结第七节），未变。若要修，需 Cache.py 与 server.py 两处共用同一解析函数。

6. **A/B Problem.py 同文件改动** — 两分支 hunk 不重叠（B 在循环开头，A 在丢失换行判断后），合并时 git 可自动合并，但建议先合 A 再 rebase B 时确认上下文行。

## 三、与原总结第六节复核

- 测试实跑结果与记录一致（11/11、5/5）。
- 拆分干净性：diff 文件集确认无交集（A 无 Cache/api.ts/ProjectCachePage；B 无 ConfigHelper/DefaultProjectConfig/ProblemAnalyzeSection）。
- 审查报告 `galtransl_pr206_review.md` 本轮已重建为**本文件**（`PR206复审报告.md`，存于 my-GalTransl 根目录）。

## 四、遗留可选项（均不阻断推送）

| 项 | 建议 |
|---|---|
| A：`threshold` useMemo 仅用于 useState 初始化 | 极轻微冗余，可留 |
| A：min/max=99 上限仅约束 spinner，手动输入 500 前后端均接受 | 口径一致，无需改 |
| B："false" 字符串解析 | 待用户决策（原待办） |
| 两分支 push / 开 PR | 等用户确认 |
