opencc
openai
packaging
PyYAML
Requests
tenacity
tiktoken
colorlog
openpyxl
orjson
beautifulsoup4
playsound3
fasttext-predict
InquirerPy
alive-progress
vaporetto
aiofiles
httpx-aiohttp
pyreqwest

# Tokenizer
budoux
# Optional tokenizers
# jieba
# pkuseg
# Very heavyweight tokenizer, manual installation recommended
# numpy < 2.0.0
# hanlp

# MCP 依赖：run_mcp_server.py（外部 agent 接入，0.5.1 起）与
# desktop/scripts/tauri_webview_mcp.py（前端 WebView 调试）共用
mcp>=2.0,<3.0  # 使用 2.x API（ServerRequestContext 等）
# 仅前端调试脚本（tauri_webview_mcp.py）需要
playwright
