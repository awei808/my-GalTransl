# GalTransl PR #206 改动总结（2026-09-09）

仓库：`D:\解包或汉化用\my-galtransl\GalTransl-stream`
基点：`c1c470b`　原分支：`fix/issue-205-linebreak-skipcheck`（`b629196`，**全程未改动**）

---

## 一、最终状态

| 分支 | 提交 | 文件 | 增量 | 推送 |
|---|---|---|---|---|
| `feat/single-sentence-too-long`（A） | `ee29657` | 7 | +242 / −1 | 未 push、未开 PR |
| `feat/skip-check`（B） | `7dc4eaa` | 7 | +193 / −3 | 未 push、未开 PR |

两分支各自保持**单提交**、无 AI 署名尾注，拆分无交叉污染
（A 不含 `Cache.py`/`api.ts`/`ProjectCachePage`；B 不含 `ConfigHelper`/`ProblemAnalyzeSection`）。

---

## 二、分支 A：单句过长检测 + 阈值可配置

| 文件 | 改动 |
|---|---|
| `GalTransl/ConfigHelper.py` | `CProblemType` 新增 `单句过长 = 13`；新增 `getAvgSentenceLengthThreshold()`：拒 bool / 拒非数字超界（`OverflowError`）/ 拒 `<=0`，统一回退 17；取整为 half-up `int(math.floor(x + 0.5))` |
| `GalTransl/Problem.py` | 新增检测：`clean_len / (n_number + 1) > threshold` 时标记「单句过长」 |
| `GalTransl/DefaultProjectConfig.py` | `problemList` 增加注释行 `- 单句过长`（**默认不启用**）；新增 `avgSentenceLengthThreshold: 17` |
| `GalTransl/server.py` | `_PROBLEM_TYPE_CATALOG` 新增条目，描述为「平均分句长度**超过**阈值」（原 PR 误写「低于」，已修正） |
| `desktop/src/pages/ProjectConfigPage.tsx` | 新增 `onThresholdChange`，把阈值写回 `problemAnalyze` |
| `desktop/src/pages/project-config/ProblemAnalyzeSection.tsx` | 阈值输入框 + 实时校验；抽出 `resolveThreshold(config)`、`DEFAULT_THRESHOLD`、`DECIMAL_LITERAL_RE` |
| `tests/test_single_sentence_too_long.py` | 7 例检测行为 + `ThresholdGuardTests` 4 例（合法值 / half-up 舍入 / 非法回退 / 缺键） |

整条链路已验证：前端改值 → `PUT /config` 整体写回 yaml → 后端 `getAvgSentenceLengthThreshold()` 读到。

---

## 三、分支 B：skip_check 跳过检查机制

| 文件 | 改动 |
|---|---|
| `GalTransl/CSentense.py` | 新增 `self.skip_check = False` |
| `GalTransl/Problem.py` | `find_problems` 循环开头：`getattr(tran, "skip_check", False)` 为真则清空 `problem` 并 `continue` |
| `GalTransl/Cache.py` | 写入：`_build_cache_obj` 用 `getattr(tran, "skip_check", False)`，仅为真时写键；读取：`get_transCache_from_json` 用 `bool(...)` 归一 |
| `GalTransl/server.py` | 保存缓存 rebuild 时 `s.skip_check = bool(e.get("skip_check", False))`；rebuild 后会 `del e["problem"]` |
| `desktop/src/lib/api.ts` | `CacheEntry` 新增 `skip_check?: boolean` |
| `desktop/src/pages/ProjectCachePage.tsx` | 卡片 pill 标记、展开区 checkbox、`handleEntryChange` 勾选时同步清 `problem` 并标记 dirty |
| `tests/test_cache_save_respects_skip_check.py` | 5 例：跳过生效 / 未跳过仍标记 / 清除旧 problem / 写缓存仅真值落盘 / 读缓存归一为 bool |

---

## 四、本次对话新增的修正

**A 分支**

1. **前后端阈值舍入分歧**：后端 `int(round(x))` → `int(math.floor(x + 0.5))`（Python `round` 是银行家舍入，JS `Math.round` 是 half-up，`.5` 值会静默差 1）。
2. **十六进制/Infinity 串**：前端 `Number()` 前加十进制字面量正则 `DECIMAL_LITERAL_RE`，拒掉 `0x10`、`Infinity` 这类「JS 收、Python 拒」的写法。
3. **阈值输入框跨项目残留**：同步输入态的 `useEffect` 依赖 `[threshold]` → `[config]`（两项目阈值同为 17 时旧值不触发同步）。
4. **改名不彻底**：测试文件 → `test_single_sentence_too_long.py`，类名 → `SingleSentenceTooLongTests`；补 4 例阈值守卫测试（此前该守卫**零覆盖**）。

**B 分支**

5. **写法统一**：`_build_cache_obj` 改 `getattr(tran, "skip_check", False)`，与 `Problem.py` 一致。
6. **改名不彻底**：测试硬编码旧名 `"长句丢失换行"` → `"残留日文"`；删除 `FakeProblemConfig` 中 feature-1 遗留的 `_threshold`/`getAvgSentenceLengthThreshold`（B 分支无消费者）。
7. **skip_check 归一**：读取改 `bool(...)`，与 `server.py` 一致；新增读取归一测试。

---

## 五、审查发现但按裁决保留 / 不修

| 项 | 结论 |
|---|---|
| 「单句过长」仍受 `n_symbol != ""` 门控（原文无换行则不检测） | **设计使然**，不改 |
| 新检测项在默认配置中是注释行（默认不启用） | **设计使然**，不改 |
| 取消勾选 skip_check 不立即恢复 problem（需保存触发 rebuild） | **设计使然**，不改 |
| `handleSave` 期间编辑会被回包覆盖（保存竞态） | **既有问题**，不修 |
| rebuild 异常时返回 `{"success": true}` 无 entries，前端内存态与磁盘不一致 | **既有问题**，不修 |

另：旧枚举名「长句丢失换行」在 BASE 上不存在，仅存在于未合并的 PR 分支，全仓无残留，**无兼容性风险**。

---

## 六、验证记录

- 测试：A **11/11**、B **5/5** 通过
- `tsc --noEmit`：A、B 均零报错
- **前后端阈值取值一致性**：从 tsx 源码正则提取 `resolveThreshold`（去 TS 类型后用 `new Function` 执行，非手抄复刻）与后端真实函数逐值比对 25 项 → `MISMATCH: NONE`
- 拆分干净性：两分支相对 BASE 的文件集无交集

---

## 七、待办 / 决策点

1. `"false"` 字符串目前按 Python `bool()` 语义解析为**真**。若需按字面解析为假，需加显式解析函数并在 Cache、server 两处共用（未做）。
2. 两分支均未 push、未开 PR，等你确认后再推。
3. 首轮产出的审查报告 `galtransl_pr206_review.md` 在仓库中未找到，如需留存可重建。
