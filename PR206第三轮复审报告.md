# PR206 第三轮复审报告（2026-09-09）

复审范围：`GalTransl-stream` 两分支相对基点 `c1c470b` 的**全部 diff 逐行人工复审**（非依赖前两轮结论）。
方法：实际读取两分支全部改动文件 + 关键调用方/消费方上下文，实跑测试与 Python 解析行为实验。

- A 分支 `feat/single-sentence-too-long`（ee29657，7 文件 +242/−1）
- B 分支 `feat/skip-check`（7dc4eaa，7 文件 +193/−3）

## 结论

两分支**无阻断性缺陷，可推送**。前两轮报告的判断均复核成立；
本次新发现 2 项值得推送前补的轻量问题（均非功能缺陷）+ 若干记录性观察。

---

## 一、基础问题排查（参数 / 依赖 / 口径 / 配置读取 / 接口对位）

| 检查项 | 结论 |
|---|---|
| A：`getAvgSentenceLengthThreshold()` 读取路径 | ✅ `projectConfig["problemAnalyze"]`，与 DefaultProjectConfig 写入位置一致（缩进已核实为 problemAnalyze 一级） |
| A：守卫异常覆盖 | ✅ TypeError/ValueError/OverflowError 全捕获；bool/≤0/NaN/inf 均回退 17 |
| A：枚举 ↔ 名称 ↔ catalog 三方一致 | ✅ `CProblemType.单句过长=13` ↔ `problemList` 写入名"单句过长" ↔ catalog 名一致；`getProblemAnalyzeConfig` 用 `CProblemType[i]` 按名映射，无缺口 |
| A：catalog 消费链路 | ✅ `/api/problem-types` 仅供配置页渲染开关与描述；`/problems` 端点聚合 `problem` 纯文本、无类型白名单 → 新类型不需额外登记，问题面板可直接显示 |
| A：Section 顶层门控 | ✅ ProjectConfigPage 顶层 `if (loading) return LoadingState`、`if (error && !config) return ErrorState` → Section 挂载时 config 恒非空，`onThresholdChange` 的 `prev ? … : prev` null 分支不可达（复核确认） |
| A：`post_dst` 在检测链路上的前提 | ✅ LLMTranslate 收尾调用 find_problems 时 `post_dst` 已由 BaseTranslate 逐行填充；Cache 读取空值 fallback `post_dst = pre_dst`。检测在翻译收尾与保存重建两条链路均有效 |
| B：skip_check 全链路 | ✅ CSentense 默认 False → Cache 写（仅真值，`getattr` 兜底）→ Cache 读（bool 归一）→ find_problems 跳过 → server rebuild（server.py:1055）→ api.ts 类型 → checkbox |
| B：rebuild 两侧 idx 对齐 | ✅ 构建与回写循环均以 `post_src == ""`（或 post_jp）同条件 continue，索引一致（逐行核对） |
| B：取消勾选后 problem 恢复 | ✅ save → rebuild → find_problems 重算 → 响应 entries 回填内存。`handleSave`/`handleSaveAll` 均以 `res.entries || 本地` 更新内存，恢复及时 |
| B：CacheEntry 类型放宽 | ✅ 全库 `onEntryChange` 调用点 4 处（pre_dst×2 / proofread_dst / skip_check）逐一核对，无类型与运行时错配 |

## 二、集成 / 状态残留 / 竞态（复核前两轮 + 新观察）

1. **B「整文件保存 vs 运行中 append」跨进程残留** —— 理论边界成立，属既有写冲突范畴，非本改动引入。（维持原裁决）
2. **B「handleSave 保存竞态」触发面被放大（新观察）** —— `handleSave` 无条件 `entriesMap.set(targetFile, savedEntries)` + `setEntries(savedEntries)`；skip_check 的"改一条→立刻保存"高频操作使"保存请求在途时继续编辑被回包覆盖"更易触发。仍属 BASE 既有竞态（未新增代码路径），但若将来优化，建议保存期间禁用编辑或合并响应而非整体覆盖。
3. **A「输入校验严于后端」** —— 复核成立：UI 只接受整数提交，后端接受小数 half-up。方向安全：前端不产生后端拒的值；yaml 手写小数时显示值（resolveThreshold 舍入）= 后端生效值，仅 yaml 文本仍存小数（不归一）。可接受。
4. **A useEffffect 依赖 `[config]`** —— 复核正确（依赖 threshold 会在跨项目同值时漏同步）。
5. **B `"false"` 字符串按 `bool()` 解析为真** —— 维持已知决策点。

## 三、本次新发现（前两轮未记录）

### 发现 1（建议补）：两分支新增 UI class 全部无对应 CSS 样式，且两分支 diff 均不含任何 styles 改动

- **B**：`.cache-card__pill--skip-check`、`.cache-card__field--skip-check`、`.cache-card__checkbox-label` 在 `project-cache.css` 中**无定义**。同文件其他 pill 变体（`--engine`/`--speaker`/`--problem`，css:537/543/549）均有专属样式。当前 ⏭ pill 只吃到基础 `.cache-card__pill` 灰色外观。
- **A**：`problem-analyze-section__threshold*` 共 6 个 class 在 `project-config.css` 中**无定义**。其中 `--error` 错误态无红色等任何专属样式（校验错误仅以无样式文本出现，视觉提示弱）。输入框靠 `base.css:25` 全局 `input` 规则兜底。

> 功能与构建均不受影响，属 UI 样式遗漏。与同文件其余变体"必有专属样式"的工程惯例不一致。建议推送前各补一段 CSS（各约 15–25 行）。

### 发现 2（备注级）：前后端守卫"逐值一致"对有限用例成立，存在未纳入 25 项比对的构造性分歧

实验证实（Python 3.12 / PyYAML）：

| 输入 | 后端 `float(raw)` | 前端 `resolveThreshold` | 分歧 |
|---|---|---|---|
| yaml 手写字符串 `"1_000"` | `float("1_000")`=1000（Python 支持下划线数字）→ 1000 | `DECIMAL_LITERAL_RE` 拒 → 回退 17 | **是** |
| yaml 值 `[17.5]`（单元素数组） | `float(list)` TypeError → 17 | `Number([17.5])`=17.5 → 18 | **是** |
| yaml 不带引号 `1_000` | PyYAML 解析为 **int 1000** → 1000 | 数字类型 → 1000 | 否 |
| `"17."` / `"+17"` / `"1e1"` | 均解析成功 | 正则放行 | 否 |
| `"0x10"` / `"Infinity"` / `"nan"` / `1e309` | 均回退 17 | 均回退 17 | 否 |

实际可达性极低（需 yaml 手写**带引号字符串**或**数组**形式的阈值），不影响正常 UI 链路。建议在 ThresholdGuardTests 追加两行注明范围，或留档即可。

### 发现 3（记录性）：`单句过长` 与 `丢失换行` 在"整段译文无换行"场景必然同时触发

译文整体丢失换行 ⇒ `n_number=0` ⇒ 全文作为一个"句子"计算 avg ⇒ 超阈值必标记。两条问题以既有 `", ".join` 机制并存（`丢失换行, 单句过长`）。非 bug、不破坏既有多标签，若产品预期"丢换行时只提示换行"，需另行约定。

### 发现 4（测试建议，均非缺陷）

- **A**：检测用例仅覆盖 `auto` + 单字符 `\n`；未覆盖显式 `linebreakSymbol`、`\\r\\n`/`\\n` 双字符字面量、尾部/连续换行。注：尾空段会被计入 `(n_number+1)` 分母 → avg 被稀释 → 方向偏保守（少报），可接受但建议补一条锁定行为。
- **B**：`rebuild_entries` helper（约 40 行）镜像复制 server.py 真实 rebuild 逻辑，**真实 handler 未被单测覆盖**，两侧易漂移。建议 helper 内加注释标注对应 server.py 行号，或后续补集成级用例。

## 四、既有文档表述复核结果

| 原表述 | 复核 |
|---|---|
| "onThresholdChange null-guard 不可达" | ✅ 成立（顶层 loading/error gate，config 单调非空） |
| "B 写缓存仅真值落盘 / 读缓存归一 bool" | ✅ 与代码一致 |
| "取消勾选 skip_check 需保存触发 rebuild 恢复 problem" | ✅ 成立，且保存响应回填内存 → 保存后立即恢复 |
| "rebuild 异常返回 success:true 无 entries" | ✅ 确认存在该分支（server.py），BASE 既有 |
| "A 输入校验严于后端，方向安全" | ✅ 成立 |
| "25 项逐值比对 MISMATCH NONE" | ⚠️ 成立但**非穷尽**，见发现 2 |

## 五、验证记录（本轮实跑）

- A 分支 11/11 通过（git worktree ee29657，跑后已清理）
- B 分支 5/5 通过
- Python float/PyYAML 边界解析实验：下划线数字、尾点、指数、十六进制、Infinity/nan 实测（见发现 2 表）
- 两分支 diff 文件集与总结一致，无交叉污染；对 Problem.py 的改动 hunk 不重叠，先合 A 再合 B 可自动合并

## 六、遗留可选项（均不阻断推送）

| 优先级 | 项 |
|---|---|
| 建议 | 补两分支新增 class 的 CSS（发现 1） |
| 建议 | B 测试 helper 标注与 server.py 的对应关系（发现 4-B） |
| 记录 | 阈值守卫构造性分歧（发现 2）、丢换行联动双标签（发现 3）、A 检测边界用例（发现 4-A） |
| 待决策 | `"false"` 字符串解析（原待办）、两分支 push / 开 PR |
