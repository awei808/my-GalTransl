# PR206 补丁复审报告（2026-09-09 晚）

复审范围：对第三轮报告意见的**落实改动**（均未提交，位于两个临时 worktree）：

- A 分支 `feat/single-sentence-too-long` → `C:/Users/19342/AppData/Local/Temp/pr206A`（4 文件未提交）
- B 分支 `feat/skip-check` → `C:/Users/19342/AppData/Local/Temp/pr206B`（1 文件未提交）

## 结论

改动质量良好，**无阻断缺陷**，逻辑正确性较上一轮进一步提升。可以提交。

验证记录：A 测试 11/11、B 测试 5/5 实跑通过；A 分支 `tsc --noEmit` 零报错（B 仅 CSS 无 TS 变更）；
从 tsx **源码提取** `resolveThreshold`（非手抄）与后端真实 `getAvgSentenceLengthThreshold` 逐值比对 **33 项 MISMATCH: NONE**
（含上一轮报告指出的全部构造性分歧用例）。

---

## 一、逐项审查

### 1. A：阈值语义收紧为「非整数一律回退 17」 ✅

第三轮报告"发现 2"（`"1_000"` / `[17.5]` 等前端正则/后端 float 构造性分歧）被以**改语义**方式消除：

- 后端 `ConfigHelper.getAvgSentenceLengthThreshold`：
  - 模块级新增 `_DECIMAL_LITERAL_RE`（与前端 `DECIMAL_LITERAL_RE` 同构，拒下划线数字），str 类型先过正则；
  - `float(raw)` 异常回退 17 保留；新增 `not math.isfinite(f) or not f.is_integer()` → 回退 17；
  - 删除 half-up 取整分支，整数直接 `int(f)`，`<=0` 回退 17。
- 前端 `resolveThreshold`：`Math.round(f)` → `Number.isInteger(f)` 判定，非整数回退 17。
- 测试：`test_rounding_is_half_up_like_js_math_round` → `test_non_integer_falls_back_to_default`（断言 17.5/2.5/16.5/字符串小数/±0.4 全回退）；invalid 集新增 `"1_000"`/`"1_0"`。

逐条核对前后端分支顺序与判定条件完全同构：
bool → str 过正则 → 非有限 → 非整数 → ≤0 → 返回值。33 项实测比对零差异（含 17.5、[17.5]、"1_000"、"17.0"、"1e1"、None、空串、0x10、Infinity/nan/inf、-0 等）。

**行为变更提示（非缺陷）**：yaml 手写小数阈值（如 17.5）由旧语义 half-up=18 变为回退默认 17。
UI 自始至终只写整数，此差异仅影响 yaml 手写场景，方向安全、可预期。合并前建议同步更新
`PR206改动总结.md` / 复审报告中关于 "half-up `math.floor(x+0.5)`" 的描述（现与代码不符）。

### 2. A：CSS 补齐 ✅

`project-config.css` 新增 6 个 threshold class 规则，覆盖 tsx 全部引用：
`.problem-analyze-section__threshold` 及 `-label/-input/-input--error/-desc/-error`。

- 引用的变量（`--space-2/3`、`--color-line/text/text-muted/primary/neutral-soft`、`--radius-control`）全部在 `tokens.css` 有定义；
- `--error` 红显（#c53030）已补，校验错误不再无样式；
- 布局采用与同区块一致的 flex + tokens 间距，风格协调；
- `-label > span`、`:focus`、`input--error` 选择器与 tsx 结构逐一对上。

### 3. B：CSS 补齐 ✅

`project-cache.css` 新增 3 段：

- `.cache-card__pill--skip-check`：`neutral-soft` 底 + muted 字，与 `--engine/--problem` 变体风格一致（分色弱于 engine，可接受）；
- `.cache-card__field--skip-check`：虚线分隔展开区，样式克制的合理处理；
- `.cache-card__checkbox-label`：inline-flex + hover 变色 + `accent-color` checkbox。

变量（含页面级 `--cache-font-xs`，定义于 project-cache.css:10）均存在；选择器与 tsx class 一一对应。

### 4. 其余核查

| 项 | 结果 |
|---|---|
| `import re` / `import math` | ✅ 均被使用（正则匹配 / `math.isfinite`），无未用导入 |
| 正则前后端同构 | ✅ 字符串完全一致（后端 `re.compile` 同款） |
| 写路径 `validateAndCommitThreshold` | ✅ 仍只接受整数且 >0，与"读路径拒小数"一致，不会产生后端拒的值 |
| 检测消费方（Problem.py） | ✅ 仅比较 `avg > threshold`，语义无耦合 |
| 注释更新 | ✅ ConfigHelper 与 tsx 注释均已改为"拒非整数"，无残留 half-up 描述 |

## 二、与第三轮报告落实对照

| 第三轮建议 | 状态 |
|---|---|
| 补两分支新增 class CSS | ✅ A、B 均已补（本报告第 2/3 节） |
| B 测试 helper 标注与 server.py 对应行号 | ⬜ 未落实（仍为镜像复制，属可选优化，不阻断） |
| 守卫构造性分歧（发现 2） | ✅ 以语义收紧方式消除，见第一节 |
| `单句过长` 与 `丢失换行` 双标签并存（发现 3） | 设计内行为，未改（预期内） |
| A 检测边界用例（发现 4-A） | ⬜ 未补（可选项） |

## 三、遗留 / 待办

1. A 分支 4 文件、B 分支 1 文件的改动均**未提交**（worktree 工作区修改），提交前建议先同步文档中 half-up 表述；
2. 第三轮"helper 标注行号"仍开放（可选）；
3. 两分支仍未 push、未开 PR，等确认。
